#include<bits/stdc++.h>
using namespace std;
void print(vector<int>v)
{
    int size=v.size();
    for(int i=0;i<size;i++)
    {
        cout<<v[i]<<" ";
    }
}
int main()
{
    vector<int>v;
    int n;
    cout<<"enter the value of n:";
    cin>>n;
    for(int i=0;i<n;i++)
    {
        int d;
        cout<<"enter the value of d:";
        cin>>d;
        v.push_back(d);
        v.clear();
        v.push_back(55);
    }
    print(v);
    return 0;
}
